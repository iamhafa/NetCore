@model IEnumerable<NetCore.Models.NhanVien>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenNhanVien)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhongBan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChucVu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenNhanVien)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhongBan.PhongBanID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChucVu)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.TenNhanVien">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TenNhanVien">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TenNhanVien">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
